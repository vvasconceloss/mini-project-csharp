@model IEnumerable<mini_project_csharp.Models.Client>

@{
    ViewData["Title"] = "Clientes";
}

<div class="container mt-4">
    <h1 class="display-4 text-center mb-4">Clientes</h1>
    <div class="text-right mb-3">
        <a href="@Url.Action("Add", "Client")" class="btn btn-primary">Adicionar Cliente</a>
    </div>
    <table class="table table-striped table-bordered">
        <thead class="thead-dark">
            <tr>
                <th>Nome</th>
                <th>Apelido</th>
                <th>Endereço</th>
                <th>Telefone</th>
                <th>Email</th>
                <th>NIF</th>
                <th>Código Postal</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var client in Model)
            {
                <tr>
                    <td>@client.Nome</td>
                    <td>@client.Apelido</td>
                    <td>@client.Endereco</td>
                    <td>@client.Telefone</td>
                    <td>@client.Email</td>
                    <td>@client.Nif</td>
                    <th>@client.IdCodPostal</th>
                    <td>
                        <a href="@Url.Action("Edit", "Client", new { id = client.IdClientes })" class="btn btn-sm btn-primary">Editar</a>
                        <a href="@Url.Action("Delete", "Client", new { id = client.IdClientes })" class="btn btn-sm btn-danger">Apagar</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>